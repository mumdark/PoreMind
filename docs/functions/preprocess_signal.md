# `preprocess_signal`
**Module:** `preprocess.py`

Dispatcher that runs selected preprocessing method.

## Parameters
- `x` (`np.ndarray`): Input signal.
- `method` (`str`): Method name.
- `**kwargs` (`Any`): Method-specific args.

## Returns
- `np.ndarray`
