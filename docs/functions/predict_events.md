# `predict_events`
**Module:** `ml.py`

Runs model package inference on event dataframe.

## Parameters
- `package` (`dict[str, Any]`): Trained model package.
- `df` (`pandas.DataFrame`): Input feature table.

## Returns
- `pandas.DataFrame with prediction columns`
