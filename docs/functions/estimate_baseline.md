# `estimate_baseline`
**Module:** `baseline.py`

Baseline estimator dispatcher (rolling_quantile/global_median).

## Parameters
- `x` (`np.ndarray`): Input signal.
- `method` (`str`): Baseline method name.
- `**kwargs` (`Any`): Method-specific args.

## Returns
- `np.ndarray`
