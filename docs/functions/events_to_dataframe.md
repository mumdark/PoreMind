# `events_to_dataframe`
**Module:** `features.py`

Converts Event list to feature dataframe.

## Parameters
- `events` (`Iterable[Event]`): Event list.
- `time` (`np.ndarray`): Time array.
- `signal` (`np.ndarray`): Signal array.

## Returns
- `pandas.DataFrame`
