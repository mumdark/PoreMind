# `median_filter`
**Module:** `preprocess.py`

Applies median filtering for impulsive-noise suppression.

## Parameters
- `x` (`np.ndarray`): Input signal.
- `window` (`int`): Window size.

## Returns
- `np.ndarray`
